# find the Maximum Element in array

# Try all possible candidates, pick the valid/best one

#array  = [2,45,124,64,1,6,75,2,345,345,2,5,6]

#maximum_value = 0
#for i in range(0, len(array)):
#    if array[i]>maximum_value:
#        maximum_value = array[i]

#print(maximum_value)
